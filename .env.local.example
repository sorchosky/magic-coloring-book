# Copy to .env.local (gitignored) for local dev with `vercel dev`.
# Also set OPENAI_API_KEY in the Vercel project's Environment Variables for
# production — never commit the real key.
OPENAI_API_KEY=
