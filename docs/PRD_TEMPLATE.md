# PRD — <Project Name>

> Fill this out by talking through it, not writing it cold. Speak the answers,
> then clean it up. This is what Claude Code reads before touching anything —
> vague sections become vague builds.

## Problem

What's broken or missing right now, for whom, and why it's worth solving.

## Users

Who uses this. If it's just you, say so — changes the bar for polish vs speed.

## Core loop

The one thing this app has to do well, described as a user action → result.
Everything else is secondary until this loop is solid.

## Scope — this version

Feature list, ordered by build priority. Each one should be scoped tight
enough that "done" is unambiguous.

1.
2.
3.

## Explicitly out of scope

Things you're deliberately not building yet, so Claude doesn't creep into them
and so you don't second-guess a scope call mid-build.

## Success criteria

How you'll know each feature above actually works. Concrete, not vibes —
"scores a 4-player game correctly from a photo" not "scoring feels good."

## Constraints

- Budget for any paid API/service:
- Timeline pressure, if any:
- Devices/browsers that matter:
- Anything that must NOT change (existing users, existing data, etc.):

## Open questions

Things you haven't decided yet. Better to name them than let Claude guess.
